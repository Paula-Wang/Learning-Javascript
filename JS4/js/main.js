//Numbers

//An integer is a whole number.
const myNumber = 42;

// A number with a decimal point is a float which references the "floating point"
const myFloat = 42.0151;

const myString = "42.123abc";

//Number Methods
//The Number.isInteger() method determines whether the passed value is an integer.

//The Number.parseFloat() method parses an argument and returns a floating point number. If a number cannot be parsed from the argument, it returns NaN. And if it finds non-numeric values it removes them and converts other data types to numeric data types.

//The toFixed() method formats a number according to how many decimal points you provide as the parameter.

//The Number.parseInt() method parses a string argument and returns an integer

//The toString() method returns a string representing the number.

//NaN is an acronym for Not a Number

//The Number .isNan() method determines whether the passed value is NaN and its type is Number.


console.log(isNaN("Pauline"));